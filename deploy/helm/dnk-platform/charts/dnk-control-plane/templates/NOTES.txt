Control Plane: {{ .Values.application.server.publicOrigin }}
Gateway Service: {{ include "dnk.controlPlane.fullname" . }}-gateway

Check rollout:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl -n {{ .Release.Namespace }} logs job/{{ include "dnk.lifecycle.jobName" . }}

Create the first administrator interactively:
  kubectl -n {{ .Release.Namespace }} exec -it deploy/{{ include "dnk.controlPlane.fullname" . }}-backend -c backend -- python src/manage.py createsuperuser

A retained migration Job runs on Helm install/upgrade and full ArgoCD Sync.
All new application pods wait for the deployment migration barrier.
Helm rollback does not roll back database migrations. Back up the database before upgrades.
Existing Secrets require a workload restart after external rotation.
StatefulSet-created PVCs survive uninstall; keep original credentials to reuse their data.
