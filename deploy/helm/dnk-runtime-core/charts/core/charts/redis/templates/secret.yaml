{{- if .Values.enabled -}}
{{- include "dnk.redis.validate" . -}}
{{- if .Values.auth.password.value }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dnk.redis.fullname" . }}-auth
  labels:
    {{- include "dnk.redis.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.auth.password.value | b64enc | quote }}
{{- end }}
{{- end }}
