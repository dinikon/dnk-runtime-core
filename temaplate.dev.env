# Local development settings
DEPLOY_ENV=DEVELOPMENT
COMMIT_SHA=dev
PROJECT__VERSION=

# Docker local runtime
API_PORT=8000

# Runtime schema defaults
SCHEMA_PREFIX=dnk_

# Database connection parts for server-based databases (default: PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=dniko
DB_CHARSET=
DB_EXTRAS=
SQLALCHEMY_DATABASE_URI_SCHEME=postgresql+asyncpg

SQLALCHEMY_POOL_SIZE=0
SQLALCHEMY_MAX_OVERFLOW=0
SQLALCHEMY_POOL_RECYCLE=0
SQLALCHEMY_POOL_USE_LIFO=false
SQLALCHEMY_POOL_PRE_PING=false
SQLALCHEMY_ECHO=true

DB_STARTUP_MAX_ATTEMPTS=5
DB_STARTUP_RETRY_DELAY_SECONDS=1

RETRIEVAL_SERVICE_EXECUTORS=2

# Identity auth settings
AUTH__OTP_CODE_LENGTH=6
AUTH__OTP_TOKEN_TTL_SECONDS=300
AUTH__SESSION_TTL_SECONDS=432000
AUTH__SESSION_COOKIE_NAME=dnk_session

# Email delivery
EMAIL__PROVIDER=smtp
EMAIL__FROM_ADDRESS=no-reply@example.com
EMAIL__FROM_NAME=DNK Runtime
EMAIL__SMTP__HOST=localhost
EMAIL__SMTP__PORT=25
EMAIL__SMTP__USERNAME=
EMAIL__SMTP__PASSWORD=
EMAIL__SMTP__USE_TLS=false
EMAIL__SMTP__USE_STARTTLS=false
EMAIL__SMTP__TIMEOUT_SECONDS=10

# Control Plane integration. HTTPS only; leave disabled until mTLS/zone acceptance.
CONTROL_PLANE__ENABLED=false
CONTROL_PLANE__PUBLIC_ORIGIN=https://core.example.test
CONTROL_PLANE__MANAGEMENT_ORIGIN=https://core-management.example.test
CONTROL_PLANE__MANAGEMENT_HOST=runtime-management.example.test
CONTROL_PLANE__INSTANCE_ID=
CONTROL_PLANE__ALLOWED_BASE_DOMAINS=["first.example.test","second.example.test"]
CONTROL_PLANE__TRUSTED_PROXY_NETWORKS=["172.30.66.2/32"]
CONTROL_PLANE__ALLOWED_CORE_FINGERPRINTS=[]
CONTROL_PLANE__INGRESS_PROBE_ADDRESS=ingress.example.test:443
# Use exactly one key source; the file must contain a persistent Fernet key.
CONTROL_PLANE__SECRET_ENCRYPTION_KEY=
CONTROL_PLANE__ENCRYPTION_KEY_PATH=
CONTROL_PLANE__CLIENT_CERT_PATH=
CONTROL_PLANE__CLIENT_KEY_PATH=
CONTROL_PLANE__CA_BUNDLE_PATH=
CONTROL_PLANE__RABBITMQ_URL=
CONTROL_PLANE__INSTALL_QUEUE=dnk.runtime.installation
CONTROL_PLANE__ACCESS_QUEUE=dnk.runtime.access
# The optional docker-compose.control-plane.yml requires these operator values.
CONTROL_PLANE_SECRET_DIRECTORY=
RUNTIME_TENANT_HOSTS=*.first.example.test *.second.example.test
PROVISIONING_RABBITMQ_USERNAME=runtime-provisioning
PROVISIONING_RABBITMQ_PASSWORD=

# Redis configuration for OTP and session token storage
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_USE_SSL=false
REDIS_DB=0

# RabbitMQ shared connection
RABBITMQ_USERNAME=rabbitmq
RABBITMQ_PASSWORD=rabbitmq
RABBITMQ_VHOST=/
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
RABBITMQ__ENABLED=false
RABBITMQ__URL=amqp://guest:guest@localhost:5672/
RABBITMQ__PUBLISHER_CONFIRMS=true
RABBITMQ__PREFETCH=20

# MinIO object storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Shared integration event bus
EVENT_BUS__EXCHANGE_NAME=dnk.integration.events
EVENT_BUS__PUBLISH_LIMIT=100
EVENT_BUS__RETRY_BASE_SECONDS=30
EVENT_BUS__MAX_ATTEMPTS=5

# Long-running outbox publisher
EVENT_BUS__PUBLISHER_WORKER_ENABLED=false
EVENT_BUS__PUBLISHER_IDLE_SLEEP_SECONDS=0.5
EVENT_BUS__PUBLISHER_ERROR_SLEEP_SECONDS=5.0

# Debug integration events console worker
EVENTS_CONSOLE_QUEUE_NAME=dnk.integration.events.console
EVENTS_CONSOLE_ROUTING_KEY=#

# Shared scheduled jobs
SCHEDULED_JOBS__PROCESS_LIMIT=100
SCHEDULED_JOBS__RECOVER_LIMIT=100
SCHEDULED_JOBS__LOCK_TTL_SECONDS=300
SCHEDULED_JOBS__RETRY_BASE_SECONDS=30
SCHEDULED_JOBS__MAX_ATTEMPTS=5
SCHEDULED_JOBS__POLL_INTERVAL_SECONDS=2
SCHEDULED_JOBS__RECOVER_INTERVAL_SECONDS=30
SCHEDULED_JOBS__LOCK_HEARTBEAT_SECONDS=60
SCHEDULED_JOBS__SHUTDOWN_GRACE_SECONDS=60
SCHEDULED_JOBS__HEARTBEAT_PATH=/tmp/dnk-cron-worker-heartbeat

# Persistent Fernet key used to encrypt partner price-list URLs.
PRICE_LISTS__SOURCE_ENCRYPTION_KEY=
PRICE_LISTS__ENCRYPTION_KEY_PATH=
