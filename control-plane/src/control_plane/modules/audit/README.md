# Audit

Owns append-only audit records for authentication changes, owner transfer,
tenant deletion, DNS/Kubernetes actions, installation credentials and platform
administration. Audit payloads must exclude OTPs, tokens and provider secrets.
