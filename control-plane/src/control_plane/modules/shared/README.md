# Shared kernel

Contains only narrow technical primitives shared across bounded contexts:
identifiers, clock, transaction boundary, pagination, request context, outbox
envelope and common infrastructure composition. Business entities do not live
here.
