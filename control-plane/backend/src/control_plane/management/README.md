# Management commands

Reserved for operational commands such as migrations, bootstrap of the first
platform administrator, reconciliation, credential rotation and audit export.
Commands must invoke application use cases instead of writing repositories
directly.
