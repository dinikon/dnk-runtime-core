# Installations

Owns cluster/single Runtime Core installations, environments, release channel,
capabilities, scoped credentials, heartbeat and `TenantPlacement`. A placement
links a global tenant to one runtime installation and records its local ID and
actual state.
