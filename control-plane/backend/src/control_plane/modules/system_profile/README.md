# System profile

Owns the singleton public platform profile: product name, public URLs, support
contacts, default locale/timezone, enabled login methods and non-secret feature
settings. Secrets are represented only by external secret references.
