# Domains

Owns normalized host bindings, ownership verification, service routing and TLS
status. Platform subdomains and custom domains use the same state machine but
different verification and DNS policies.
